Build a public auto-publishing blog site in the current directory.

Goal:
Create a complete static blog system for an alter-ego author focused on AI + logistics. It should generate and publish a new post every 10 minutes. Each post must include: date/time, title, a roughly 150-word listicle-style main piece, and a thread-style "more thoughts" section on the same topic. Also create a fictional council of critics that appears on the site as recurring reviewer personas.

Defaults to use unless clearly impossible:
- Site name: choose something sharp and memorable in the AI/logistics lane.
- Make up a strong pen name for the author.
- Dark theme, clean editorial look.
- Public GitHub Pages-compatible static site.
- Keep stack simple and robust. Prefer plain HTML/CSS/JS or a tiny Node build over a heavy framework.

Requirements:
1. Generate an initial set of sample posts so the site is not empty.
2. Build a content generation script that can create one new post every 10 minutes.
3. Build a site render pipeline so generated posts appear on the homepage and individual post pages if appropriate.
4. Include the critic council with short bios and per-post critic reactions if that fits the design.
5. Include timestamping in America/Los_Angeles.
6. Add a scheduler path for local automatic publishing every 10 minutes on this Mac, ideally launchd, with clear setup/start scripts.
7. Add git automation so a generated post commits and pushes to origin after generation.
8. Keep secrets out of the repo. Use environment variables or local config for any API keys/provider settings.
9. Make it easy to swap model/provider later.
10. Add a README with exact run/start/stop instructions.

Important constraints:
- Do not ask for stack choices unless blocked. Make sensible defaults.
- Prefer a solution that actually works on this machine with minimal dependencies.
- If live AI generation requires a provider key, support multiple env-based providers and create a graceful fallback/manual mode.
- The deliverable must be ready for a public GitHub repo and GitHub Pages.
- Do not push anywhere. Prepare the repo locally only.

Done criteria:
- The project directory contains a working static site and generator scripts.
- There is at least one generated post visible in the rendered site output.
- There are scripts/configs for a 10-minute local publishing loop.
- README explains how to run it and what env vars are needed.
- Verify key files exist and summarize how the system works.

When completely finished, run this command exactly:
openclaw system event --text "Done: logistics alter-ego blog scaffold built in /Users/ra_bot/projects/logistics-alter-ego-blog" --mode now
